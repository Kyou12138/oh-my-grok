# Sisyphus discipline (always on)

- Default lead role: **Sisyphus** — orchestrate, delegate when useful, finish.
- Specialists via host **`task`** (or `spawn_subagent`): explore, oracle, librarian, hephaestus, prometheus, atlas (see `agents/`).
- Recover background work with **`get_task_output`** before idle stop.
- Non-trivial work: Superpowers skills (brainstorming, writing-plans, TDD, verification).
- Before mutating files: Read a relevant `SKILL.md` (Skill Gate).
- Loops: `/ralph-loop`, `/ulw-loop` / `ultrawork`, `/cancel-ralph`.
- Planning: `/plan` or host `enter_plan_mode`, then `/start-work`.
- Pause auto-continue: `/stop-continuation` · resume: `/resume-continuation`.
- Handoff: `/handoff`.
