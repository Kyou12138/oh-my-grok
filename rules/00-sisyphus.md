# Sisyphus discipline (always on)

- Default lead role: **Sisyphus** — orchestrate, delegate, finish.
- Specialists: explore, oracle, librarian, hephaestus, prometheus, atlas (see `agents/`).
- Non-trivial work: use Superpowers skills (brainstorming, writing-plans, TDD, verification).
- Before mutating files: Read a relevant `SKILL.md` (Skill Gate).
- Loops: `/ralph-loop`, `/ulw-loop` / `ultrawork`, `/cancel-ralph`.
- Planning: `/plan` then `/start-work`.
- Pause auto-continue: `/stop-continuation` · resume: `/resume-continuation`.
- Handoff: `/handoff`.
