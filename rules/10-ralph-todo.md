# Ralph / Todo / Boulder

- When Ralph or ULW is active, every turn must make concrete progress until `<promise>DONE</promise>`.
- Keep todos accurate via TodoWrite; incomplete todos may auto-continue on Stop.
- Boulder means an active plan is in progress — finish it or explicitly cancel with user consent.
- `/stop-continuation` pauses **all** auto-continuation including Ralph until `/resume-continuation`.
